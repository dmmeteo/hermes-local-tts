# Open questions

1. What warm real-time factor and peak RSS does StyleTTS2 single achieve on the target VPS?
2. Does the selected voice pass the user's listening test on short and medium Ukrainian responses?
3. Should the first production runtime be a persistent localhost HTTP worker or a supervised command worker?
4. Which parts of KobzarAI preprocessing are license-compatible and useful without bilingual routing?
5. Can `/tts <text>` return media through the current plugin command result contract without core changes?
6. What model revision and dependency versions are stable on Linux CPU?

Questions 1–2 are the immediate frontier.
